# Namedle

A wordle spin-off where player guesses names instead of words!!

## Setup
1. Clone this repo on to your local machine by either using `git clone ` or by downloading this repo as a zip
2. Run main.exe by typing `./main.exe` in the terminal
3. Enjoy the game!

## How to play Namedle
